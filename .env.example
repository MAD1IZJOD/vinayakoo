# Copy to .env.local and fill in. Never commit real values.

# JSON form endpoint that receives consultation requests (for example a Formspree form URL).
# When unset, the form opens a pre-filled email to the studio instead.
VITE_CONSULT_ENDPOINT=
